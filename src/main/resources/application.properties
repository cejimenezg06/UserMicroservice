# Config General
# Change localhost to database image name (localhost -> user_db)
spring.datasource.url=jdbc:postgresql://localhost:5432/users_ms_db
spring.datasource.username=postgres
spring.datasource.password=password
spring.jpa.show-sql=true
spring.jpa.generate-ddl=true
spring.jpa.hibernate.ddl-auto=update

# Change Port to 6000
server.port=6500